package Model;

import java.awt.BasicStroke;
import java.awt.Color;
// import java.awt.Graphics2D;

public class Square extends Rectangle {

    public Square(int x1, int y1, int side, Color color, BasicStroke stroke, Color fillColor, boolean transparent) {
        super(x1, y1, side, color, stroke, fillColor, transparent);
    }
}